mixin card(title, price, img)
    .card
        .card__img
            img(src="img/" + img , alt= title, width = "180" + px)
        .card__title
            | #{title}
        .card__price
            | #{price}